# Full stack TS starter with React client and Express server

Client:

- tanstack router
- tailwind
- import aliases
- api proxy

Server:

- cors setup for client
- zod-express-middleware
- dockerfile to deploy
- caddy configuration

To install deps and run (from parent directory):

```
pnpm run install
pnpm run dev
```
